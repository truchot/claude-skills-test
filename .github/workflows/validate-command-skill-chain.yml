name: Validate Command-Skill Chain

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']
  workflow_dispatch:

jobs:
  validate-chain:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate command → skill references
        run: bash .claude/tests/validate-command-skill-chain.sh

      - name: Validate routing commands
        run: bash .claude/tests/validate-routing-commands.sh
