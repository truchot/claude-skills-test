{
  "name": "experience-client-tests",
  "version": "1.0.0",
  "description": "Tests de validation pour le skill experience-client",
  "private": true,
  "type": "module",
  "comment": "Zero external dependencies - uses only Node.js built-ins (fs, path, test)",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:verbose": "node --test --test-reporter spec tests/*.test.js",
    "test:domains": "node --test tests/validate-domains.test.js",
    "test:agents": "node --test tests/validate-agents.test.js",
    "test:skill": "node --test tests/validate-skill.test.js",
    "test:validators": "node --test tests/validate-validators.test.js"
  },
  "keywords": ["claude-skills", "experience-client", "tests"],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
